[project]
name = "sync-engine"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "you@example.com"}
]
requires-python = ">=3.14"
dependencies = [
    "watchdog == 6.0.0",
    "colorlog == 6.10.1",
    "pydantic == 2.12.5",
    "Flask == 3.1.3",
    "requests == 2.33.1",
]

[tool.poetry.scripts]
client = 'sync_engine.client.__main__:main'
server = 'sync_engine.server.__main__:main'

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest == 9.0.3",
]
